import React from "react";
import styled from "styled-components";

const HeaderContainer = styled.div`
    background-color: darkgray;
    font-size: x-large;
`

const Header = (props)=>{

    return(
        <HeaderContainer>Header Container</HeaderContainer>
    );
};

export default Header;